{
  "name": "@timetracker/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "bash -lc 'rm -rf .tmp-test && tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json && node --test .tmp-test/test/**/*.test.js; code=$?; rm -rf .tmp-test; exit $code'"
  }
}
